{% extends "MascaEtudiantBundle::layout.html.twig" %}
{% block title %}Reinscription universitaire{% endblock %}
{% block etudiant_body %}
    <h3> Page de reinscription de {{ universitaire.person.nom }} {{ universitaire.person.prenom }} </h3>
    {% if error_message is defined %}
        <p style="color: red "> {{ error_message }}</p>
    {% endif %}
    <div class="row">
        <div class="span12">
            <fieldset>
                <legend>Étude</legend>
                <form method="post">
                    {{ form_widget(sonFiliereForm) }}
                    <div class="submit-style">
                        <div class="floatleft">
                            <a href="{{ path('details_universite', {'id': universitaire.id}) }}">
                                <img title="Annuler" src="{{ asset('assets/cancel.png') }}">
                            </a>

                        </div>
                        <div class="floatright">
                            <input type="image" src="{{ asset('assets/save.png') }}" title="Enregitrer">
                        </div>
                    </div>
                </form>
            </fieldset>
        </div>
    </div>
{% endblock %}