{% extends "MascaEtudiantBundle::layout.html.twig" %}
{% block title %}Regularisation ecolage{% endblock %}
{% block etudiant_body %}
    {% if error_message is defined %}
        <p style="color: red "> {{ error_message }}</p>
    {% endif %}
    <div class="row bordered">
        <div class="span12 text-center">
            <p>Régularisation d'écolage de {{ sonFiliere.universitaire.person.nom }} {{ sonFiliere.universitaire.person.prenom }}</p>
            <p>Annee d'etude {{ sonFiliere.anneeEtude }}</p>
            <p>Le montant d'un mois est {{ montant }} Ar</p>
            <p>Reduction {{ reduction }} Ar</p>
            {% if reste is defined %}
                Reste: {{ reste }}
            {% endif %}
        </div>
    </div>
    <div class="row">
        <div class="span12 text-center">
            <form method="post" class="text-left" style="width:270px; margin:0 auto !important;" id="formpaiement">
                {{ form_widget(form) }}
                <div class="align-center">
                    <input type="submit" value="Enregistrer" class="btn btn-primary">
                    <a href="{{ path('ecolage_universitaire', {'id':sonFiliere.id}) }}" class="btn btn-primary">Annuler</a>
                </div>
            </form>
        </div>

    </div>
{% endblock %}