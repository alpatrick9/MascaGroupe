{% extends 'MascaEtudiantBundle::layout.html.twig' %}
{% block etudiant_body %}
    <h3>Page d'enregistrement/modification d'absence</h3>
    <p>Etudiant concerné: <b>{{ universitaire.person.prenom }} {{ universitaire.person.nom }}</b></p>
    {% if error_message is defined %}
        <p style="color: red "> {{ error_message }}</p>
    {% endif %}
    <form method="post">
        {{ form_widget(form) }}
        <div class="align-right">
            <input type="submit" value="Enregistrer" class="btn">
            <a href="{{ path('absence_universitaire',{'id':universitaire.id}) }}" class="btn">Annuler</a>
        </div>
    </form>
{% endblock %}