{% extends "MascaEtudiantBundle::layout.html.twig" %}
{% block title %}Modification université{% endblock %}
{% block etudiant_body %}
    <form method="post">
        <div class="row">
            <div class="span6">
                <fieldset>
                    <legend>Infos personnelle</legend>
                    {{ form_widget(personForm) }}
                </fieldset>
            </div>
            <div class="span6">
                <fieldset>
                    <legend>Infos conmplementaire</legend>
                    {{ form_widget(infoEtudiantForm) }}
                </fieldset>
            </div>
        </div>
        <div class="align-right">
            <input type="submit" value="Enregistrer" class="btn btn-primary">
            <a href="{{ path('details_universite', {'id':universiteId}) }}" class="btn btn-primary">Annuler</a>
        </div>
    </form>
{% endblock %}