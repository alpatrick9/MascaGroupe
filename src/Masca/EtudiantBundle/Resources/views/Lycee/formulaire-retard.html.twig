{% extends 'MascaEtudiantBundle::layout.html.twig' %}
{% block etudiant_body %}
    <h3>Page d'enregistrement/modification de retard</h3>
    <p>Etudiant concerné: <b>{{ lyceen.person.prenom }} {{ lyceen.person.nom }}</b></p>
    {% if error_message is defined %}
        <p style="color: red "> {{ error_message }}</p>
    {% endif %}
    <form method="post">
        {{ form_widget(form) }}
        <div class="align-right">
            <input type="submit" value="Enregistrer" class="btn">
            <a href="{{ path('retard_lyceen',{'id':lyceen.id}) }}" class="btn">Annuler</a>
        </div>
    </form>
{% endblock %}